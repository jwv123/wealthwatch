{
  "buildCommand": "cd test/backend && npm install && npm run build && cd ../frontend && npm install && npx ng build --configuration production",
  "outputDirectory": "test/frontend/dist/wealthwatch/browser",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}